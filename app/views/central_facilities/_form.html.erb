<%= simple_form_for(@central_facility) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

    <div class="form-actions">
      <%= f.button :submit, :class => "btn btn-large btn-success" %>
      <%= link_to 'ย้อนกลับ', facilities_path, :class => "toolbar-back" %>
    </div>
<% end %>
